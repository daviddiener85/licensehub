# Specifications

Product specification documents are stored here as source artifacts so the repository keeps a durable history of requirements.

## Current Documents

- `license_hub_spec_v1_7.docx` - saved on 2026-05-11. Introduces the auto-populated digital mandate form, phone signature capture, ID photo embedding and generated signed PDF workflow.

## Tracking Notes

- Add each new specification version to this directory.
- Record the date, source version and main changes in `docs/WORK_LOG.md`.
- When implementation work starts from a specification change, add a new dated work-log entry describing what was built or modified.
